input_dim: 32  # number of numeric + encoded categorical features after selection
num_classes: 2
swin:
  model_name: swin_tiny_patch4_window7_224
  pretrained: false
  embed_dim: 96
  img_adapter: 4   # channels for feature-to-image adapter
  img_size: 224
  drop_rate: 0.1

gat:
  hidden_dim: 64
  num_heads: 4
  num_layers: 2
  dropout: 0.2

fusion_dim: 128
